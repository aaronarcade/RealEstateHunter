{
  "name": "realestatehunter",
  "version": "0.1.0",
  "private": true,
  "description": "Schema validation tooling for RealEstateHunter property artifacts.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "validate-property": "node scripts/validate-property.mjs",
    "property-status": "node scripts/property-status.mjs",
    "create-property": "node scripts/create-property.mjs",
    "test": "node --test scripts/validate.test.mjs scripts/property-cli.test.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
