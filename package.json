{
  "name": "realestatehunter",
  "version": "0.1.0",
  "private": true,
  "description": "Schema validation tooling for RealEstateHunter property artifacts.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test scripts/validate.test.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
